export const BASE_URL = new URL(process.env.BASE_URL || "http://localhost");
export const OIDC_URL = new URL(process.env.OIDC_URL || "http://localhost");
export const API_URL = new URL(process.env.API_URL || "http://localhost");
